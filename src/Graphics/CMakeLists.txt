set(GRAPHICS_CORE SDLWindow SDL_GL_Window GLDraw)
set(GRAPHICS_DIRECTX  SDL_D3D_Window D3DDraw)
if(WIN32)
	set(GRAPHICS_SOURCES ${GRAPHICS_CORE} ${GRAPHICS_DIRECTX})
else(WIN32)
	set(GRAPHICS_SOURCES ${GRAPHICS_CORE})
endif(WIN32)

add_library(Graphics ${GRAPHICS_SOURCES})
